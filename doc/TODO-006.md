
### TODO-006

Add a new route/page that contains the following:

* build timestamp
* version (for now, start with 0.0.9)
* last git commit hash (abbreviated)
    * this does not have to account for the commit that updates the version info

Note: assume that the user (a developer) will know to navigate to the page. No need to place on the home page.